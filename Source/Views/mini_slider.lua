class('MiniSlider').extends(playdate.graphics.sprite)

--todo - copy VerticalSlider, but horizontal
function MiniSlider:init(x, y, value, rangeStart, rangeEnd, listener, labelIsFloat)
	MiniSlider.super.init(self)

end